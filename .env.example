# OpenAI API Configuration (REQUIRED for root cause analysis)
OPENAI_API_KEY=your_openai_api_key_here

# Flask Secret Key (REQUIRED for sessions)
SECRET_KEY=your_secret_key_here

# Email Configuration (Optional - already configured in code)
SMTP_HOST=smtp.mailjet.com
SMTP_PORT=587
SMTP_USER=your_mailjet_api_key
SMTP_PASS=your_mailjet_secret_key
EMAIL_SENDER=your_email@example.com
EMAIL_RECIPIENT=recipient_email@example.com

# Server Configuration
PORT=5000
